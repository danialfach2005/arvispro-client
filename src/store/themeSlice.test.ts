import { describe, it } from "vitest";

describe.skip("themeSlice (Fitur Dihapus)", () => {
  it("themeSlice telah dihapus dari aplikasi", () => {
    // Dummy test agar vitest tidak error (No test suite found)
  });
});
